import avoidanceHeuristics from '../src/services/avoidanceHeuristics.js';

describe('avoidanceHeuristics stub module', () => {
  test('exports an empty object for backward compatibility', () => {
    expect(avoidanceHeuristics).toEqual({});
  });
});
