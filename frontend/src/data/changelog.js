export const APP_VERSION = '1.9.79-preview';

export const changelog = [
  {
    version: '1.9.79-preview',
    date: 'April 22, 2026',
    entries: [
      { type: 'fixed', text: 'Auto-assigned and noise-dismissed runs silently vanished from the Decided / Dismissed tabs -- both tabs filter by sinceField=\'decidedAt\', but _executeRun never set decidedAt on pipeline-finalized decisions (only admin actions via /decide + /dismiss did). So runs with decidedAt=NULL never satisfied the 24h/7d/30d/All time-range filter, making them invisible to anyone trusting those tabs to be a complete picture. History tab still showed them because it queries createdAt, masking the issue. Root cause verified on prod: 1 of 2 auto_assigned runs and 26 of 28 noise_dismissed runs had decidedAt=NULL. Fix: _executeRun now stamps decidedAt=now() when decision is auto_assigned or noise_dismissed. Admin-triggered decisions unchanged (they already set decidedAt). pending_review stays NULL until an admin actually decides' },
      { type: 'fixed', text: 'freshServiceActionService.execute now clears decidedAt when downgrading an auto_assigned run to pending_review on preflight failure -- previously decidedAt stayed stamped from the original auto-assign, which would falsely surface the downgraded run in the Decided tab even though nobody had actually approved it' },
      { type: 'database', text: 'Migration 20260422200000_backfill_decided_at_auto_decisions backfills decidedAt=updatedAt for all historical auto_assigned + noise_dismissed runs that currently have decidedAt=NULL. Deliberately excludes pending_review (which should stay NULL). Already applied to prod before this release; run `prisma migrate deploy` on any other environments before deploying' },
      { type: 'improved', text: 'Extracted isPipelineFinalDecision() pure helper to assignmentDecisionRules.js with 5 unit tests covering auto_assigned, noise_dismissed, pending_review, admin-only outcomes (approved/modified/rejected), and defensive null/undefined/unknown handling. Suite total: 64 tests (was 59). Module coverage stays at 100%' },
    ],
  },
  {
    version: '1.9.78-preview',
    date: 'April 22, 2026',
    entries: [
      { type: 'new', text: 'Rich empty-state panel on the Review Queue when Auto-Assign is on -- the page used to render a sad "No tickets awaiting decision" placeholder when the queue was empty, which made it look like nothing was happening even though the pipeline was busy auto-assigning tickets in the background. Replaced with a hero stats panel showing today\u2019s auto-assigned count, plus a 4-tile breakdown (Handled in FS, Noise dismissed, Needed review, In progress), and a "most recent auto-assignment" preview strip with a quick link to the run detail. Includes a contextual mention of how many groups are excluded so the admin remembers why some tickets WILL still appear here. Old empty-state shows when Auto-Assign is off (no behavior change there)' },
      { type: 'new', text: 'GET /assignment/queue-status now returns autoAssign / autoCloseNoise / pipelineEnabled / dryRunMode / excludedGroupCount / today stats, all in one shot. Today stats are workspace-timezone scoped (so "today" matches what the coordinator considers the current shift) and cover totalRuns, autoAssigned, approved, handledInFs, noiseDismissed, manualReviewRequired, inProgress, queuedForLater, plus the most-recent auto-assignment preview' },
      { type: 'improved', text: 'Pure stats aggregation logic extracted to assignmentStatsAggregation.js (tallyGroupedRuns + adjustForHandledInFs) so the Prisma groupBy -> bucket mapping is unit-tested. 18 new tests covering empty/null inputs, all bucket assignments, mixed realistic days, and the handled-in-FS subtraction edge cases. Module at 100% line coverage' },
    ],
  },
  {
    version: '1.9.77-preview',
    date: 'April 22, 2026',
    entries: [
      { type: 'improved', text: 'Renamed the amber decision pill from "FS Manual" to "Handled in FS" -- the old label read like a field name from a form rather than a user-facing status, and the word "Manual" felt like the admin still had to do something. "Handled in FS" makes it clear that the work is already in motion outside the pipeline and no in-app decision is needed. Tooltip is unchanged ("Assigned in FreshService outside the pipeline \u2014 <tech>. AI suggestion left unresolved."). Applies to both the list view pill in Decided > Manually in FreshService and the header pill on the run detail page' },
    ],
  },
  {
    version: '1.9.76-preview',
    date: 'April 22, 2026',
    entries: [
      { type: 'fixed', text: 'Decided tab showed "Pending" instead of "FS Manual" for externally-assigned tickets once they closed -- the decision-label logic (getDisplayDecision in AssignmentReview + externallyAssigned check in PipelineRunDetail) had a status guard excluding Closed/Resolved/Deleted/Spam tickets, but the backend "outside_assigned" filter (that powers the "Manually in FreshService" sub-tab) had no such guard. So Closed tickets that had been manually assigned in FreshService appeared in the sub-tab but displayed the wrong label. Removed the status guard in both UI sites -- if a pending_review run has an assignedTechId, it\u2019s always "FS Manual" regardless of current ticket status. Keeps list view and detail view consistent' },
    ],
  },
  {
    version: '1.9.75-preview',
    date: 'April 22, 2026',
    entries: [
      { type: 'fixed', text: 'Excluded Groups picker showed "0 agents" for every group -- the backend was reading `g.agent_ids` from the FreshService /groups response, but FS actually returns group members under `members` (with separate `observers` and `leaders` arrays). The agent_ids field the code expected is a Freshdesk-ism that the Freshservice API has never populated on /groups responses. Picker now reads `members` with an `agent_ids` fallback for defensiveness, so counts render correctly (e.g. "Everyone IT" now shows 15 agents instead of 0)' },
      { type: 'fixed', text: 'Pre-existing FreshService preflight "incompatible_group" check was silently always-true -- same root cause: `if (group.agent_ids && !group.agent_ids.includes(agentId))` short-circuited to false on every call because agent_ids was always undefined, meaning the check never actually caught agents being assigned to groups they didn\u2019t belong to. Fixed to read `members` (with agent_ids fallback) so the preflight finally does its job. Low impact in practice because the LLM rarely recommended cross-group assignments, but closes a silent-failure hole for future cases' },
    ],
  },
  {
    version: '1.9.74-preview',
    date: 'April 22, 2026',
    entries: [
      { type: 'new', text: 'Per-group exclusion from auto-assignment -- Configuration > Excluded Groups (Manual Approval) lets admins pick FreshService groups (e.g. catch-all groups like "Everyone IT") that should always require manual approval, even when Auto-Assign is on. The LLM still produces a recommendation for each ticket; the admin just has to click approve in the Review Queue before it gets written back to FreshService' },
      { type: 'new', text: 'Live group picker in Configuration -- new section live-fetches the FreshService group list each time the config tab opens (GET /assignment/groups), shows agent count per group, and supports filtering when there are more than 8. Selections survive across sessions; if FreshService is unreachable the picker falls back to showing the currently-saved group IDs as opaque chips so the admin doesn\u2019t lose their choices' },
      { type: 'new', text: 'Manual approval banner on excluded-group runs -- when _executeRun downgrades an auto-assigned run to pending_review because the ticket\u2019s group is on the excluded list, PipelineRunDetail surfaces a blue "Manual approval required" strip near the top with the group context. Joins the existing amber rebound strip and red auto-fallback-exhausted strip introduced in v1.9.73' },
      { type: 'database', text: 'New migration 20260422000000_add_excluded_group_ids -- adds excluded_group_ids INTEGER[] NOT NULL DEFAULT \'{}\' column to assignment_configs. Already applied on prod before this release; run `prisma migrate deploy` on any other environments before deploying' },
    ],
  },
  {
    version: '1.9.73-preview',
    date: 'April 21, 2026',
    entries: [
      { type: 'new', text: 'Auto-fallback for rejected auto-assignments -- when an agent rejects an auto-assigned ticket (removes themselves in FreshService), the existing rebound flow now wires through end-to-end: the LLM gets explicit Rebound Context in its prompt, knows which agent rejected via the previouslyRejectedThisTicket flag from find_matching_agents, picks the next-best candidate while actively avoiding the rejecter, and the new assignee\u2019s agentBriefingHtml acknowledges the re-routing with a neutral "this ticket was returned to the queue" line (no naming the previous assignee, no mention of the algorithm). Up to 3 successive auto-fallbacks per ticket; after that a pending_review run is materialized for manual handling' },
      { type: 'new', text: 'Rebound Context block in the LLM\u2019s first user message -- _executeRun now loads reboundFrom from the run record (already persisted by syncService for v1.9.5+) and injects a "## Rebound Context" section telling the LLM which attempt this is, when the previous return happened, and how to handle the briefing. Previously this metadata sat on the run record but never reached the LLM, so it had to discover the rebound implicitly by noticing rejection flags on candidates' },
      { type: 'new', text: 'DEFAULT_SYSTEM_PROMPT learns about rebounds -- Step 4 (Find Matching Agents) instructs the LLM to exclude prior rejecters unless they\u2019re the only qualified option, and never make a rejecter the rank-1 pick if any alternative exists. Step 8 (agentBriefingHtml) instructs the briefing to start with a brief neutral acknowledgement when this is a rebound run, without naming the previous assignee or using the words "rebound", "bounced", or "rejected". A new needsPromptUpgrade detector + replaceAgentBriefingStep path auto-upgrades older published prompts on next run (preserves any custom Steps 1-7)' },
      { type: 'improved', text: 'Loop-guard exhaustion no longer goes silent -- when MAX_AUTO_REBOUNDS_PER_TICKET (3) is hit, _handleTicketRebound used to logger.warn and return, leaving the ticket invisible. Now it materializes a decision=pending_review run with triggerSource=rebound_exhausted, a synthesized "no candidates left to try" recommendation, and the latest reboundFrom snapshot, so the ticket appears in Awaiting Decision with a clear "needs manual handling" red strip' },
      { type: 'fixed', text: 'No more stuck "auto_assigned + syncStatus=failed" runs -- two failure paths previously left runs in a confusing dead state where the dashboard showed the ticket as assigned but FreshService was unchanged. (1) If the LLM ignores the rebound prompt and re-suggests the prior rejecter, _executeRun now downgrades to pending_review before the FS round-trip with an explanatory errorMessage. (2) If the FreshService preflight blocks the assign with already_rejected_by_this_agent / superseded_assignee / incompatible_group, freshServiceActionService now downgrades the auto-assigned run to pending_review (clearing assignedTechId) so it surfaces in Awaiting Decision instead of looking falsely successful. Manually-approved runs keep their original decision so the audit trail of admin intent stays intact' },
      { type: 'improved', text: 'Rebound context is finally visible in PipelineRunDetail -- amber strip near the top of the run detail page reads "Rebound #N \u2014 returned from <tech> at <time>" with a one-line explanation that the pipeline was re-run with explicit instructions to avoid the rejecter. Red strip variant for rebound_exhausted runs reads "Auto-fallback exhausted after N rebounds \u2014 needs manual review". Reuses the RotateCcw icon from the existing rejection metric on TechCard' },
    ],
  },
  {
    version: '1.9.72-preview',
    date: 'April 21, 2026',
    entries: [
      { type: 'fixed', text: 'Run Now on a queued ticket no longer disappears into a 5-second toast -- clicking Run Now in the "Queued for Business Hours" section now opens a slide-over with the live LLM analysis streaming in real time (text, tool calls, recommendation cards), the same view you get from the Manual Trigger panel. Closing the overlay doesn\u2019t cancel the run -- it continues server-side -- and the queue refreshes automatically so the row moves to Awaiting Decision (or Decided / Dismissed) without a manual reload' },
      { type: 'new', text: 'POST /assignment/runs/:id/run-now?stream=true -- new SSE streaming variant of the existing run-now endpoint that mirrors the /trigger?stream=true contract, so LivePipelineView could be reused unchanged. Falls back to the original fire-and-forget 202 response when stream=true is omitted (preserves the previous API contract for any other callers)' },
      { type: 'improved', text: 'When a queued run is auto-rejected at validation (e.g. ticket got assigned externally between queueing and Run Now), the SSE stream now emits an "error" event with code=skipped_stale and the reason text, so the live overlay can show "Skipped: Ticket already assigned to a technician" instead of dying on a 409 the EventSource can\u2019t read' },
      { type: 'improved', text: 'LivePipelineView gains three optional props (streamPath, skipExistingCheck, initialRunId) so it can be embedded in flows where the run has already been claimed server-side (run-now) without racing the "is there a recent run?" probe that exists for the manual-trigger flow' },
    ],
  },
  {
    version: '1.9.71-preview',
    date: 'April 21, 2026',
    entries: [
      { type: 'new', text: 'Sanitized agent-facing assignment notes -- the FreshService private note posted on auto-assign / approval no longer leaks our internal scoring (rank, score, confidence, "fairness", competing candidates, workload counts, proficiency levels, on-shift status, rebound history). The submit_recommendation tool now produces a separate agentBriefingHtml -- a short HTML message written directly to the assignee in plain language ("you\u2019ve handled similar VPN tickets recently") with allowed tags only (b, i, br, p, ul, li, a, h3) -- which is what gets posted to the ticket. The internal overallReasoning is preserved in the DB / admin UI for audit but never shipped to FS' },
      { type: 'new', text: 'Closure-notice rewriting for noise tickets -- when the pipeline auto-closes a ticket as non-actionable, the closing FS note is now an LLM-written closureNoticeHtml ("This appears to be an automated notification with no action required") instead of the raw classification + algorithmic reasoning that previously got posted' },
      { type: 'new', text: 'Public-note preview card on Assignment Review -- the run detail page (both pending-review and history tabs) now shows a green "What the agent will see" card with the rendered HTML, so admins can spot leaks before approving; falls back to an amber warning card on legacy runs without the new field, telling the admin a sync would use the old fallback so they can re-run' },
      { type: 'new', text: 'Half-day vacation leaves handled end-to-end -- VT marks partial-day leaves with isFullDayLeave=false and a startHour/endHour window, but we were collapsing them into a full-day row, which made the LLM assigner skip agents who were only out for the morning or afternoon. Sync now bucketises partial leaves into AM/PM by window-midpoint vs noon (09:00-13:00 = AM, 12:30-16:30 = PM); get_agent_availability emits HALF-DAY-OFF / HALF-DAY-WFH buckets with derived availabilityNote ("off this morning, available from 12:00 onward"); default prompt updated so half-day agents are treated as available for the other half' },
      { type: 'improved', text: 'Half-day cells in the dashboard mini-calendar render as split day-cells (top half = AM, bottom half = PM) with the ticket count overlaid; daily badge reads "OFF AM" / "WFH PM"; MonthlyCalendar leave counts are weighted (1 for full, 0.5 for half) and rendered as 1/2, 1 1/2, 2; cells use a vertical gradient that fades from the leave colour at the AM/PM edge through the midline so they no longer look unfinished' },
      { type: 'improved', text: '"FS Manual" badge for externally-assigned runs -- when a ticket is assigned in FreshService outside the pipeline, the run still has decision=pending_review technically, but no human action is needed in our app. List rows and the run detail header now render an amber "FS Manual" pill instead of yellow "Pending"; tooltip explains "Assigned in FreshService outside the pipeline -- <tech name>. AI suggestion left unresolved." Render-layer change only; the underlying decision data is unchanged so coordinators can still record their own decision later if they want to capture their assessment of the AI suggestion vs the actual external pick' },
      { type: 'fixed', text: 'Rebound dedup gap that produced two pending_review runs for the same ticket -- _handleTicketRebound\u2019s dedup check used getOpenPipelineRun, which only matches status IN (queued, running), so a pending_review run wasn\u2019t "open" by that definition and the rebound proceeded to create a second pending row (seen on prod ticket #219505, shown twice in Awaiting Decision). Fix: in the rebound path, after the in-flight dedup check and before creating the new run, mark any existing pending_review runs for the ticket as "superseded" with reason "Superseded by a newer rebound run after ticket was returned to the queue". Superseded runs still exist in the History tab for audit' },
      { type: 'database', text: 'New migration: add is_full_day, half_day_part, start_minute, end_minute columns to technician_leaves (additive, defaults preserve existing behaviour); run `prisma migrate deploy` before deploying this release' },
    ],
  },
  {
    version: '1.9.7-preview',
    date: 'April 21, 2026',
    entries: [
      { type: 'fixed', text: 'Demo Mode broken avatars -- getDemoAvatar() was using `(hashString(key) ^ seed) % files.length` where the XOR returns a SIGNED 32-bit int, so JS\u2019s sign-preserving modulo could produce a negative index and the URL became /demo-avatars/undefined; the broken <img> then rendered the alt text which leaked the (real or fake) name. Fixed by forcing unsigned with `>>> 0` before the modulo; added a 1000-key probe in the smoke test so this never regresses' },
      { type: 'fixed', text: 'Demo Mode name leaks in Assignment Review and Timeline Explorer -- the AI Suggestion column was rendering `recommendation.recommendations[].techName` and the timeline rows were rendering `ticket.assignedTechName`, neither of which were in the scrubber\u2019s NAME_KEYS set, so real technician names were displayed even with Demo Mode on; added techName, technicianName, assignedTechName, _techName, currentHolderName, holderName, pickerName, fromTechName, toTechName, rejectedByName, lastHolderName, previousHolderName' },
      { type: 'improved', text: 'Defensive image fallback in Demo Mode -- TechCard, TechCardCompact, and TimelineTicketRow now hide the <img> on load error instead of letting the browser show its broken-image placeholder + alt text; ensures any future bad avatar URL fails to initials gracefully without leaking the technician name' },
    ],
  },
  {
    version: '1.9.6-preview',
    date: 'April 21, 2026',
    entries: [
      { type: 'new', text: 'Demo Mode for screen recordings -- one-click toggle in the Dashboard header anonymizes every sensitive string on screen (technician names, requester names, emails, office locations, ticket subjects, computer names, internal domains) so you can record training videos without manual editing; persists in localStorage so it survives page refreshes' },
      { type: 'new', text: 'Stock face avatar pool -- 50 photo-realistic corporate headshots generated with Gemini 3 Pro Image (Nano Banana Pro) bundled under /demo-avatars/; technician profile photos are deterministically replaced from the pool when Demo Mode is on, with a Reshuffle option in the toggle dropdown to mid-session re-randomize the roster' },
      { type: 'new', text: 'Per-session deterministic identity mapping -- the same real person always maps to the same fake person within a recording (no flicker between pages or live updates), but a brand new browser tab/recording starts with a fresh roster; uses a sessionStorage seed + Mulberry32 PRNG so identities are reproducible without any persistent storage of the mapping itself' },
      { type: 'new', text: 'DEMO MODE banner -- amber pill fixed to the bottom-right of every page when Demo Mode is on, so you never accidentally record real data thinking the toggle was active' },
      { type: 'improved', text: 'Single-chokepoint anonymization -- axios response interceptor and SSE event handler both route data through a recursive scrubber when Demo Mode is on, so all pages (Dashboard, Technician Detail, Timeline Explorer, Assignment Review, Visuals, Settings) pick up the scrub automatically with zero per-page changes' },
      { type: 'improved', text: 'Smart subject scrubbing -- ticket subjects are processed through an ordered regex pipeline (emails -> computer names -> internal domains -> known locations -> known people -> trigger-based generic name catcher) that preserves tech jargon like "Significant Anomaly" or "Application or Service Principal" while still catching unfamiliar names from "New Hire: <X>" / "involving <X>" / "for <X>" patterns' },
      { type: 'improved', text: 'Map view location remap -- when Demo Mode is on, agent.location values are swapped to other valid Canadian cities (Halifax, Winnipeg, Hamilton...) that already exist in the Visuals page office lookup, so map pins move to plausible-but-different cities automatically without any map code changes' },
      { type: 'improved', text: 'Timezone city scrubbing -- IANA timezones like America/Toronto are remapped to other valid IANA names (America/Halifax, America/Phoenix...) so the city portion of the timezone string no longer leaks the technician\u2019s real office while date math continues to work normally' },
    ],
  },
  {
    version: '1.9.5-preview',
    date: 'April 17, 2026',
    entries: [
      { type: 'new', text: 'Assignment bounce tracking -- new ticket_assignment_episodes table captures every ownership period per ticket (who held it, how it started, how it ended); "self-picked" semantics now reflect the current owner, so if a tech picks then drops a ticket the next holder gets correct attribution' },
      { type: 'new', text: 'Rejected (Rej) metric on technician cards -- red badge with RotateCcw icon appears when a tech has rejected tickets in the last 7 days; tooltip shows 7d / 30d / lifetime counts; click the badge to drill into the list' },
      { type: 'new', text: 'Bounced tickets tab on technician detail -- new tab shows every ticket this tech picked up and put back in the queue, filterable by 7 days / 30 days / lifetime, with the current holder and hold duration for each' },
      { type: 'new', text: 'Handoff history strip on run-detail page -- horizontal timeline above the recommendation cards shows every pickup/rejection/reassignment chain for the ticket, so coordinators see at a glance who held it and why it came back' },
      { type: 'new', text: 'Live freshness check for pending runs -- the review page now re-queries FreshService when you open it and surfaces warnings: "assignee changed", "recommended tech already rejected this ticket", "tech is not a member of the ticket group"; admins get a "Refresh & re-rank" button to supersede stale runs with a fresh pipeline' },
      { type: 'new', text: 'Preflight validation on FreshService write-back -- before approving a recommendation, the backend checks live FS state and blocks with clear reasons (superseded_assignee, incompatible_group, already_rejected_by_this_agent); force flag available to override when needed' },
      { type: 'new', text: 'Historical episode backfill endpoint -- new POST /api/sync/backfill-episodes for admins to populate the episodes table with up to 180 days of FreshService activity history; existing admin Backfill panel now also writes episodes automatically' },
      { type: 'improved', text: 'Shared FreshService rate limiter -- single token-bucket queue on FreshServiceClient caps at 110 req/min, adapts to x-ratelimit-remaining header, and honors Retry-After on 429s; replaced per-callsite throttling that was causing overlapping retries and bursts across workspaces (dev testing: rate-limit hits dropped 99.8%, from 1,843 to 3)' },
      { type: 'improved', text: 'Full FreshService error body captured -- failed syncs now persist the complete FS validation error (errors[] array) in syncPayload.freshserviceError instead of discarding it, making it obvious why a sync failed' },
      { type: 'improved', text: 'LLM rejection awareness -- find_matching_agents tool now annotates each candidate with previouslyRejectedThisTicket and rejectedAt, giving the recommendation prompt a soft signal to avoid re-suggesting an agent who already bounced the ticket' },
      { type: 'improved', text: 'New ticket_activities event types -- sync now writes real FreshService events (self_picked, coordinator_assigned, rejected, reassigned, group_changed) with the actor name, replacing the previous "System" placeholder; foundation for future per-ticket activity timeline views' },
      { type: 'improved', text: 'Diagnostics endpoint -- GET /api/sync/rate-limit-stats returns current FreshService budget, queue depth, current min-delay, and slowdown status for real-time observation of the limiter' },
      { type: 'fixed', text: 'Vacation Tracker in-place modification bug -- when a user edited an existing VT leave in place (same leave ID, different dates -- e.g. moved WFH from Thursday to Friday), the old date rows were orphaned and the user appeared on leave for both days; deleteStaleLeaves now keys on (vtLeaveId, leaveDate) tuples so modifications properly clear stale days' },
      { type: 'fixed', text: 'Assignment approval race condition -- run #340 style failures (approving a recommendation after the ticket was self-picked then rejected outside the pipeline) now caught at preflight with a specific error code instead of silently failing with "Validation failed" from FreshService' },
      { type: 'fixed', text: 'Sync missed intermediate assignment changes -- previous sync filter only fetched FS activities when the ticket was currently assigned, so a brief pick-and-drop window between polls would never be recorded; filter broadened to also refetch when FS updated_at > our local timestamp or the ticket has an active pipeline run' },
      { type: 'database', text: 'New migration: ticket_assignment_episodes table (PK id, FK ticket_id/technician_id/workspace_id, started_at, ended_at, start_method, end_method, unique (ticket_id, started_at)); new columns on tickets (rejection_count, group_id); new index on ticket_activities(activityType); run `prisma migrate deploy` before deploying this release' },
    ],
  },
  {
    version: '1.9.4-preview',
    date: 'April 12, 2026',
    entries: [
      { type: 'new', text: 'App-assigned ticket tracking -- dashboard now distinguishes tickets assigned by Ticket Pulse (via FreshService API) from self-picked and coordinator-assigned tickets; new "App" stat with Bot icon appears on TechCard, TechCardCompact, and dashboard header when app assignments exist' },
      { type: 'new', text: 'Service Account Names setting -- configurable comma-separated list of FreshService agent names used by the app (Settings > FreshService Configuration); assignments by these agents are counted as "App Assigned" across daily, weekly, and monthly views' },
      { type: 'new', text: 'Stacked bar visualization updated -- Self-Picked vs Assigned bar in technician detail now includes a sky-blue "App" segment between Self (blue) and Assigned (gray) when app assignments are present' },
      { type: 'improved', text: 'Dashboard stat cards -- new "App" card with Bot icon in the header stat bar (sky-blue theme), shown conditionally when there are app-made assignments; legend updated to distinguish "App = App Assigned" from "Asgn = Coordinator Assigned"' },
      { type: 'improved', text: 'Stats calculator refactored -- all stat functions (daily, weekly, monthly, technician detail, dashboard aggregate) accept a serviceAccountNames parameter and produce separate appAssigned counts; case-insensitive matching for service account names' },
      { type: 'improved', text: 'Frontend filter recalculation -- search and category filters correctly recalculate app-assigned counts using the serviceAccountNames from the API response; daily breakdown grids include appAssigned per day' },
      { type: 'improved', text: 'Dashboard header clickable day overview tiles for technician navigation' },
      { type: 'improved', text: 'Assignment queue UX -- Status and AI Suggestion split into separate columns; row opacity no longer dims the quick-approve popover' },
      { type: 'improved', text: 'Calibration UX polish -- prompt-only mode for quick runs, cancel button for stuck runs, smart auto-scroll, SSE heartbeat to prevent Azure timeouts, KB progress streaming, and draft prompt delete support' },
      { type: 'fixed', text: 'Deleted/spam ticket reconciliation -- tickets that vanish from FreshService list API due to soft-delete or spam flagging are now detected and marked correctly during sync' },
    ],
  },
  {
    version: '1.9.3-preview',
    date: 'April 9, 2026',
    entries: [
      { type: 'new', text: 'Calibration Run system -- kick off an automated self-improvement process over a selectable date range that classifies every assignment outcome (top recommendation, in pool, or outside pool), analyzes patterns in mismatches and admin feedback to generate a draft prompt update, and runs targeted competency re-evaluation for technicians with discrepancies' },
      { type: 'new', text: 'Calibration tab in Ticket Assignment (admin-only) -- date-range picker with presets (last 2 weeks, last month, last 3 months), live streaming progress through data collection, prompt analysis, and per-tech competency phases, and a history list of past calibration runs' },
      { type: 'new', text: 'Outcome classification engine -- auto-compares ticket.assignedTechId (from FreshService sync) against pipeline recommendation pool to determine if the actual assignee was the top recommendation, in the pool, or outside the pool entirely' },
      { type: 'new', text: 'Prompt self-improvement -- calibration LLM analyzes outcome-3 cases and admin decision notes to find routing patterns, then generates a complete updated assignment prompt saved as a draft version for admin review in Prompt Manager' },
      { type: 'new', text: 'Calibration-driven competency updates -- technicians who were assigned tickets outside recommendations or whose top-recommendation was repeatedly overridden are flagged and run through the existing competency analysis pipeline with enriched calibration context' },
      { type: 'new', text: 'get_calibration_signals competency tool -- new tool available during calibration-triggered competency analysis that surfaces the specific assignment signals (tickets handled outside recs, tickets where tech was recommended but passed over) for the technician being analyzed' },
      { type: 'improved', text: 'Shared SSE streaming infrastructure -- extracted readSSEStream utility and useStreamingFetch hook from duplicated fetch/reader/buffer code; LivePipelineView, CompetencyManager, and CalibrationManager all share the same SSE plumbing' },
      { type: 'database', text: 'New migration: calibration_runs table for tracking calibration run state, outcome classifications, prompt findings, and competency update progress; run `prisma migrate deploy` before deploying this release' },
    ],
  },
  {
    version: '1.9.2-preview',
    date: 'April 8, 2026',
    entries: [
      { type: 'improved', text: 'Assignment pipeline audit hardening -- end-to-end review of queue, decision, sync, and polling flows closed multiple state gaps so the pipeline behaves deterministically instead of relying on operator workarounds' },
      { type: 'fixed', text: 'Open-run concurrency guard -- queued/running assignment runs are now protected by a database partial unique index on ticket_id, and pipeline start/queue paths gracefully recover when a concurrent request already claimed the ticket' },
      { type: 'fixed', text: 'Long-running pipeline heartbeat -- active LLM runs now refresh the parent run heartbeat during streaming and tool execution so stale-run cleanup no longer marks healthy runs as failed mid-analysis' },
      { type: 'fixed', text: 'Decision transition enforcement -- approve/modify/reject/dismiss actions now require a completed run that is still pending review, preventing double-decides and late mutations on already-resolved runs' },
      { type: 'fixed', text: 'Manual FreshService sync safety -- sync and sync-preview now require a syncable completed decision, and already-synced runs reject repeat real writes unless an explicit force flag is used' },
      { type: 'fixed', text: 'Email polling dedupe repaired -- mailbox-triggered assignment checks now use the same active-run guard as the rest of the pipeline instead of calling a missing repository method' },
      { type: 'fixed', text: 'Retry polling for failed assignment runs -- post-sync unassigned ticket polling now retries only when the latest pipeline run failed, was cancelled, was superseded, or was marked stale; pending/completed runs no longer block or duplicate reprocessing incorrectly' },
      { type: 'fixed', text: 'Deleted-ticket reconciliation -- when a FreshService ticket disappears, pending-review assignment runs are now superseded automatically so reviewers cannot keep acting on dead tickets' },
      { type: 'improved', text: 'Queued business-hours drain resiliency -- queued assignment runs are now drained during business hours even if the preceding sync cycle failed, reducing unnecessary queue stalls' },
      { type: 'security', text: 'Assignment review UI now matches reviewer RBAC -- non-admin reviewers no longer see invalid dismiss/delete/run-now/manual-trigger/sync controls that the backend would reject' },
      { type: 'improved', text: 'Queue copy and status messaging aligned with real behavior -- "Delete all" now accurately describes the bulk action, FreshService sync controls honor admin access, and Pending ticket status is treated consistently across queue/detail views' },
      { type: 'improved', text: 'Live pipeline empty-state wording clarified -- runs with no technician recommendations now explain the outcome without implying every such run was auto-dismissed' },
      { type: 'database', text: 'New migration: partial unique index on assignment_pipeline_runs(ticket_id) for rows with status IN (queued, running); run `prisma migrate deploy` before deploying this release' },
    ],
  },
  {
    version: '1.9.1-preview',
    date: 'April 6, 2026',
    entries: [
      { type: 'new', text: 'Reviewer RBAC role -- new workspace role between viewer and admin; reviewers can view the assignment queue, approve/reject recommendations, and add triage notes but cannot access admin settings, trigger pipelines, or manage competencies/prompts' },
      { type: 'new', text: 'Enhanced Review Queue -- Pending/Assigned/All sub-view tabs with counts, time-range filters (24h/7d/30d/All), and ticket status filters (Open/Assigned/Closed with live counts) to focus on actionable tickets' },
      { type: 'new', text: 'Decision page redesign -- ticket subject is now the prominent heading with requester, department, priority badge, category tag, and clickable FreshService link; run metadata demoted to secondary line' },
      { type: 'new', text: 'Staleness detection banner -- amber warning appears on pending tickets that were assigned outside the pipeline or changed status since analysis, with the assigned tech name shown' },
      { type: 'new', text: 'Recommendation cards with avatars -- technician photos, location with MapPin icon, visual separator before reasoning text; data loaded from technician API on mount' },
      { type: 'new', text: 'Shift-aware agent availability -- LLM now sees each agent timezone, local time, on-shift status, and hours remaining; system prompt updated with on-shift preference as decision rule' },
      { type: 'new', text: 'Queue business hours countdown -- amber pill shows "Starts Tomorrow at 5 AM PT" with pulsing dot; green pill when business hours are active; human-friendly time format with timezone abbreviations' },
      { type: 'new', text: 'Pending queue shows suggested agent and analysis timestamp for each ticket awaiting review' },
      { type: 'improved', text: 'Mobile-friendly dashboard header -- compact layout with prominent Assignment button, icons-only navigation, stats bar stacked vertically with Daily/Weekly/Monthly toggle and ticket count' },
      { type: 'improved', text: 'Queue table tightened -- text-xs font, compact padding, Status column showing Open/Assigned/Closed badges inline with assigned tech name for pre-triaged tickets' },
      { type: 'improved', text: 'Post-decision redirect -- approving or rejecting now returns to the queue list instead of staying on the run detail page' },
      { type: 'improved', text: 'Queue styling softened -- heavy orange replaced with amber tones, inline status pill on the right, cleaner table backgrounds' },
      { type: 'fixed', text: 'Stats bar overlap on mobile -- no longer sticky on small screens, date navigation buttons enlarged for touch' },
      { type: 'security', text: 'Reviewer role restricted from destructive operations -- run-now, delete, bulk-delete, dismiss, sync, and sync-preview routes require admin; reviewers limited to read + decide' },
      { type: 'security', text: 'Viewer access guard on Assignment page -- viewers see a clean "Access Restricted" page instead of an empty confusing state' },
      { type: 'security', text: 'History detail view now read-only -- decision panel properly hidden (onDecide=null instead of no-op handler)' },
      { type: 'database', text: 'Pending queue query expanded to include ticket.status, ticket.assignedTechId, and ticket.assignedTech for inline staleness detection' },
      { type: 'database', text: 'Production prompt upgraded to v10 with shift-aware availability step injected into custom prompt' },
    ],
  },
  {
    version: '1.9.0-preview',
    date: 'April 6, 2026',
    entries: [
      { type: 'new', text: 'Mobile-responsive assignment pages -- Review Queue, Pipeline Analysis, and Decision Panel are fully usable on phones; icons-only tab bar, card layout for queue, viewport-relative heights, 44px touch targets, and touch-manipulation on all buttons' },
      { type: 'new', text: 'Split-view decision panel -- recommendations on the left (60%), decision actions on the right (40%) with sticky positioning; replaces the old stacked layout' },
      { type: 'new', text: 'Triage notes on every decision -- optional note field on approve, reject, and override decisions; saved to database and injected into LLM feedback context for continuous improvement' },
      { type: 'new', text: 'LLM decision history search -- new search_decision_notes tool lets Claude search past admin triage notes to learn routing preferences and patterns; system prompt updated with Step 5b for decision history' },
      { type: 'new', text: 'AI Assignment button on dashboard header -- purple pill-shaped button with Sparkles icon next to Timeline Explorer for quick access to the assignment pipeline' },
      { type: 'new', text: 'Shared streaming components -- CopyBadge, ToolCallCard, StreamContent, and processStreamEvent extracted into StreamingComponents.jsx; both Pipeline Analysis and Competency Analysis use the same rendering module' },
      { type: 'new', text: 'Inline thinking indicator during pipeline streaming -- shows "processing X.X KB..." with spinner during silent gaps when Claude is building tool inputs' },
      { type: 'improved', text: 'Pipeline streaming events persist after completion -- tool cards and text no longer vanish when the stream ends; formatted events take priority over raw transcript on revisit' },
      { type: 'improved', text: 'Clean transcript formatting on revisit -- raw [Tool: name] JSON dumps replaced with readable blockquote summaries; old *(KB)* markers stripped' },
      { type: 'improved', text: 'Tech override picker moved to right decision panel -- searchable list with avatars, locations, and "recommended" badges opens at the top of the panel, not at the bottom' },
      { type: 'improved', text: 'Competency matrix shows technicians even with zero categories -- empty-state message guides users to add categories or run LLM analysis' },
      { type: 'improved', text: 'Prompt auto-upgrade preserves custom prompts -- injects new tool steps into existing customized prompts instead of replacing them with the default' },
      { type: 'fixed', text: 'Pipeline streaming blank page after completion -- events gated behind streaming boolean disappeared when fetch ended; changed to events.length check' },
      { type: 'fixed', text: 'FreshService sync crash on deleted tickets -- 404/405 errors from notes and close API calls now handled gracefully; sync marked as successful with informational note' },
      { type: 'fixed', text: 'Sync retry 405 crash -- axios interceptor wrapped errors in ExternalAPIError before catch blocks could inspect status; now 404/405 pass through unwrapped' },
      { type: 'fixed', text: 'Email test crash when Graph API returns non-JSON error -- testConnection wrapped JSON.parse in try/catch like getUserProfile already does' },
      { type: 'fixed', text: 'SSE Connecting status for non-admin users on production -- JWT query parameter promoted to Authorization header before requireAuth runs' },
      { type: 'fixed', text: 'Competency Prompt tab blank screen -- missing Wrench icon import after modularization refactor' },
      { type: 'security', text: 'Anthropic API key and Azure Graph credentials deployed to production App Service environment variables -- no longer missing from server configuration' },
      { type: 'security', text: 'Decision notes stored server-side only -- triage context saved to assignment_pipeline_runs.decision_note column, never exposed beyond the admin who wrote it' },
      { type: 'database', text: 'New column: assignment_pipeline_runs.decision_note (TEXT) -- stores admin triage context for any pipeline decision; migration applied to production' },
      { type: 'database', text: 'Production competency data synced -- 25 categories and 182 technician competency mappings copied from dev to prod by FreshService ID matching' },
      { type: 'database', text: 'Production prompt upgraded to v9 -- search_decision_notes step injected into custom prompt, preserving all admin customizations from v8' },
    ],
  },
  {
    version: '1.7.0-preview',
    date: 'April 5, 2026',
    entries: [
      { type: 'new', text: 'AI-powered ticket assignment pipeline -- Claude analyzes incoming tickets, classifies them, evaluates technician competency/workload/location, and recommends the best assignee with full reasoning; supports manual review, auto-assign, and dry-run modes' },
      { type: 'new', text: 'Assignment Review page -- dedicated UI to view pipeline recommendations, approve/modify/reject assignments, see step-by-step LLM reasoning with token usage, and sync approved assignments back to FreshService' },
      { type: 'new', text: 'Live Pipeline View -- real-time streaming view of pipeline runs as they execute, showing each step (classification, categorization, availability, competency matching, recommendation) with live transcript' },
      { type: 'new', text: 'Competency analysis pipeline -- LLM analyzes each technician\'s 180-day ticket history to automatically build competency profiles with proficiency levels (basic/intermediate/expert); results can be rolled back' },
      { type: 'new', text: 'Competency Manager UI -- view and edit technician competencies per workspace, trigger LLM analysis runs, review analysis history with before/after snapshots, and roll back changes' },
      { type: 'new', text: 'Prompt Manager -- versioned prompt editor for both assignment and competency pipelines; draft/publish/archive workflow with restore capability; auto-seeds default prompts on first use' },
      { type: 'new', text: 'Assignment pipeline configuration -- per-workspace settings for LLM model, scoring weights (competency/workload/location/recency), polling for unassigned tickets, max recommendations, and dry-run mode' },
      { type: 'new', text: 'Email monitoring for assignment pipeline -- optional Microsoft Graph mailbox polling to detect new tickets via email and trigger assignment pipeline runs automatically' },
      { type: 'new', text: 'Pipeline queue with business-hours gating -- tickets arriving outside business hours are queued with reason and automatically processed when business hours resume' },
      { type: 'new', text: 'FreshService assignment sync -- approved pipeline recommendations can be synced to FreshService to actually assign the ticket; tracks sync status (pending/synced/failed/dry_run)' },
      { type: 'new', text: 'Feedback learning from admin overrides -- when an admin modifies a pipeline recommendation, the override reason is accumulated in feedbackContext and injected into future LLM prompts for continuous improvement' },
      { type: 'new', text: 'Dashboard calendar shows database-stored holidays -- workspace-specific and shared holidays displayed on the calendar with proper styling' },
      { type: 'new', text: 'Agent location sync from Azure AD -- technician locations (city, state, country) are synced from Azure AD alongside photos, with 35+ Canadian cities mapped correctly' },
      { type: 'new', text: 'Detailed sync report -- photo/location sync now shows a comprehensive report with force-overwrite option for locations' },
      { type: 'improved', text: 'SSE connectivity for non-admin users -- JWT query parameter is now promoted to Authorization header before requireAuth runs, fixing persistent "Connecting..." status for viewers' },
      { type: 'improved', text: 'Middleware chain corrected -- requireAuth now runs before requireWorkspaceAccess, ensuring session user is populated from JWT before workspace access checks' },
      { type: 'improved', text: 'Workspace access check uses live DB lookup instead of stale session cache, preventing stale role data after access changes' },
      { type: 'improved', text: 'Business hours and LLM settings panels fixed -- components now use authenticated API client with workspace headers instead of raw axios' },
      { type: 'improved', text: 'Map visualization shows 35+ Canadian cities correctly instead of only 5 hardcoded ones' },
      { type: 'fixed', text: 'Changelog modal crash when opening on certain versions' },
      { type: 'fixed', text: 'Workspace access panel improvements for edge cases' },
      { type: 'fixed', text: 'Workspace switch causing unnecessary logout -- default compact view preserved across workspace changes' },
      { type: 'fixed', text: 'Azure AD user search uses Graph API search with ConsistencyLevel header and fallback for reliable results' },
      { type: 'fixed', text: 'Express 4 async middleware compatibility -- converted async middleware to promise chains to prevent unhandled rejections' },
      { type: 'fixed', text: 'Holiday seeding in database with comprehensive Canadian holiday list' },
      { type: 'security', text: 'Anthropic API key configuration added for Claude-powered pipelines -- stored server-side only, never exposed to frontend' },
      { type: 'security', text: 'Azure Graph API credentials (tenant ID, client ID, client secret) added for backend email monitoring -- used for Microsoft Graph mailbox polling' },
      { type: 'database', text: 'New tables: assignment_configs, competency_categories, technician_competencies, assignment_pipeline_runs, assignment_pipeline_steps, assignment_prompt_versions, competency_prompt_versions, competency_analysis_runs, competency_analysis_steps -- full assignment and competency pipeline infrastructure' },
      { type: 'database', text: 'New columns on assignment_pipeline_runs: full_transcript, prompt_version_id, sync_status, synced_at, sync_error, sync_payload, feedback_applied, queued_at, queued_reason, claimed_at' },
      { type: 'database', text: 'New columns on assignment_configs: monitored_mailbox, email_polling_enabled, email_polling_interval_sec, last_email_check_at, auto_close_noise, dry_run_mode' },
      { type: 'database', text: 'New column on ticket_assignments: pipeline_run_id (FK to assignment_pipeline_runs)' },
      { type: 'database', text: 'Run `prisma migrate deploy` on production before deploying. Prompts are auto-seeded on first pipeline use.' },
    ],
  },
  {
    version: '1.6.0-preview',
    date: 'April 3, 2026',
    entries: [
      { type: 'new', text: 'Workspace access control -- new requireWorkspaceAccess middleware enforces that users can only access workspaces they have been granted access to; global admins bypass the check' },
      { type: 'new', text: 'Per-workspace admin roles -- requireAdmin now checks workspace_access.role in addition to the global admin list; workspace admins can manage settings for their workspace without global admin privileges' },
      { type: 'new', text: 'Workspace Access panel in Settings -- admin UI to grant/revoke user access per workspace with viewer or admin roles; visible only to workspace admins' },
      { type: 'new', text: 'Role-based Settings navigation -- viewers see only Dashboard config; workspace admins see Sync, Noise Rules, LLM, Vacation Tracker, etc.; global admins see FreshService credentials, Workspace Management, and Admin Management' },
      { type: 'new', text: 'Role badges in workspace picker -- dropdown shows "[admin]" or "[viewer]" next to each workspace name so users can see their access level' },
      { type: 'new', text: 'Shared technician workspace indicator -- new API endpoint GET /api/settings/technicians/:id/workspaces shows which workspaces a technician appears in' },
      { type: 'new', text: 'Ticket assignment scaffolding -- TicketAssignment model added to database (not yet active in UI); FreshService assignTicket() API wrapper prepared; PERMISSIONS constants defined for future use' },
      { type: 'security', text: 'All sensitive mutation routes now require admin -- sync trigger/reset/backfill, noise rule CRUD, LLM config mutations, visuals edits, photo sync, and settings updates are all gated behind requireAdmin' },
      { type: 'security', text: 'Workspace selection enforced -- POST /api/workspaces/select now verifies the user has access before allowing workspace switch; previously any authenticated user could switch to any workspace by ID' },
      { type: 'security', text: 'Default workspace fallback removed -- requests without explicit workspace context now return 400 instead of silently defaulting to workspace 1' },
      { type: 'improved', text: 'LLM config workspace scoping -- autoResponseService, llmService, and availabilityService.calculateETA now pass the correct workspace ID to getPublishedConfig() instead of defaulting to workspace 1' },
      { type: 'improved', text: 'Auto-response records now include workspaceId on creation, ensuring proper workspace isolation' },
      { type: 'improved', text: 'Technician getById hardened -- ticket includes now filter by the technician workspace, preventing potential cross-workspace data leakage from DB inconsistencies' },
      { type: 'database', text: 'New table: ticket_assignments -- stores assignment history with ticket, technician, assigner email, workspace, source (manual/auto/freshservice), and optional notes; migration applied' },
    ],
  },
  {
    version: '1.5.2-preview',
    date: 'March 31, 2026',
    entries: [
      { type: 'fixed', text: 'Tech Visibility panel now loads reliably -- replaced heavy query (loaded all tickets + requesters for every technician) with a lightweight query that fetches only the fields needed; eliminates timeouts on large workspaces' },
      { type: 'fixed', text: 'Database connection stability -- removed prisma.$disconnect() calls from photo sync route handlers that were severing the shared DB connection for the entire application; visiting Profile Photos no longer causes other pages to lose data on refresh' },
      { type: 'fixed', text: 'Noise filter toggle reliability -- sync service now re-evaluates isNoise/noiseRuleMatched for every ticket during upsert using workspace-scoped rules; backfilled IT workspace (2,809 noise tickets updated); Open/Pending counts now visible in weekly view header for clearer filter feedback' },
      { type: 'fixed', text: 'Backend crash prevention -- 404 routes now use operational NotFoundError instead of generic errors that triggered restart warnings; added placeholder /api/notifications endpoint to prevent recurring 404s' },
      { type: 'improved', text: 'Tech Visibility panel shows clear error messages with Retry button when loading fails (auth errors, network issues) instead of silently showing an empty list' },
      { type: 'improved', text: 'Frontend code cleanup -- removed 20+ debug console.log statements from Dashboard, DashboardContext, SSE hooks, Visuals, and LLM panels; all sync progress is now communicated through the UI sync log panel' },
      { type: 'improved', text: 'Resolved all 81 ESLint errors across frontend -- fixed unused imports, unescaped JSX entities, indentation issues, and constant conditions in BackfillPanel, SyncOperationsPanel, VacationTrackerPanel, Dashboard, TimelineExplorer, and WorkspacePicker' },
      { type: 'database', text: 'No schema changes -- all fixes are code-level. No migration or prisma db push required for this release.' },
    ],
  },
  {
    version: '1.5.1-preview',
    date: 'March 27, 2026',
    entries: [
      { type: 'new', text: 'Hourly Vacation Tracker sync -- leave data is now automatically refreshed every hour via a scheduled cron job; sync results are logged and visible in Settings > Sync Operations alongside FreshService sync logs' },
      { type: 'improved', text: 'Dashboard row alignment fixed -- technician name and badges (SELF, OFF, WFH, OTH) are now in separate fixed-width columns so weekly day cells always align vertically regardless of badge count' },
      { type: 'improved', text: 'Dashboard total count display -- removed misleading "+" prefix, vertically centered with indigo color, consistent across Cards and Compact views' },
      { type: 'improved', text: 'Workspace auto-retry on login -- if the workspace list fails to load during authentication (e.g., slow DB startup), the app automatically retries fetching workspaces instead of showing an empty list' },
      { type: 'improved', text: 'Database connection pooling -- session store capped at 5 connections, Prisma pool limited to 10 connections; prevents connection exhaustion during development restarts and ensures stable production operation' },
      { type: 'fixed', text: 'Technician visibility persists across syncs -- FreshService sync no longer overwrites admin-set isActive status; disabled technicians stay hidden permanently until re-enabled in Settings' },
      { type: 'fixed', text: 'Production data migration -- existing tickets, technicians, and sync logs correctly assigned to workspace 1 (IT); non-IT FreshService agents cleaned up from IT workspace view' },
      { type: 'database', text: 'Connection pool limits added to prevent pg_use_reserved_connections errors under load' },
    ],
  },
  {
    version: '1.5.0-preview',
    date: 'March 27, 2026',
    entries: [
      { type: 'new', text: 'Multi-workspace support -- discover, activate, and switch between FreshService workspaces from a workspace picker in the dashboard header; each workspace has independent technicians, tickets, sync schedules, and settings; workspace selection persists across sessions' },
      { type: 'new', text: 'Workspace Management panel in Settings -- discover all FreshService workspaces, activate/deactivate with one click, auto-provisions default business hours, LLM config, and noise rules for new workspaces' },
      { type: 'new', text: 'Vacation Tracker integration -- sync leave data from vacationtracker.io to show who is OFF, WFH, or on training directly on the dashboard; per-workspace API key, leave type categorization (OFF/WFH/OTHER), and automatic user matching by email' },
      { type: 'new', text: 'Leave indicators on dashboard -- amber badges for OFF (Vacation/PTO/Sick), teal for WFH, purple for OTHER; visible in daily view (name badge), weekly view (colored day cells with dots), and monthly calendar (leave count pills per day)' },
      { type: 'new', text: 'Technician Visibility panel in Settings -- permanently enable/disable technicians per workspace with toggle switches; disabled technicians are hidden from all dashboard views; survives FreshService sync cycles' },
      { type: 'new', text: 'Admin Management panel in Settings -- manage admin email addresses via UI; admins can add/remove other admins (cannot remove themselves)' },
      { type: 'new', text: 'Backfill panel in Settings -- import historical ticket data with timeframe presets, real-time progress tracking, and skip-existing option' },
      { type: 'new', text: 'Per-workspace configurable category custom field -- each workspace can use a different FreshService custom field for ticket categorization (e.g., IT uses "security", Accounting uses "case_type")' },
      { type: 'improved', text: 'Dashboard ticket total display redesigned -- removed misleading "+" prefix, centered layout with indigo color, label changed from "WEEK" to "total"' },
      { type: 'improved', text: 'All dashboard queries are now workspace-scoped -- technicians, tickets, sync logs, noise rules, and settings are filtered by the active workspace' },
      { type: 'improved', text: 'Technician sync no longer overwrites manual isActive changes -- the FreshService sync preserves admin-set visibility, only setting isActive on first creation' },
      { type: 'improved', text: 'Dashboard read cache is cleared when technician visibility is toggled, ensuring immediate UI updates' },
      { type: 'security', text: 'Workspace isolation -- all API routes enforce workspace boundaries via middleware; workspace ID passed via X-Workspace-Id header and validated server-side' },
      { type: 'database', text: 'New tables: workspaces, workspace_access, vacation_tracker_configs, vt_leave_types, vt_user_mappings, technician_leaves -- workspace_id foreign key added to all tenant-scoped tables with automatic backfill to workspace 1 (IT)' },
      { type: 'database', text: 'Technician unique constraint changed from freshservice_id to composite (freshservice_id, workspace_id) to support same agent across multiple workspaces' },
    ],
  },
  {
    version: '1.4.2-preview',
    date: 'March 10, 2026',
    entries: [
      { type: 'new', text: 'Sync Operations panel in Settings — new dedicated section with a full Sync Health dashboard (4 stat cards: total syncs, success rate, last sync, longest gap) and a 48-hour visual timeline strip showing sync coverage vs gaps; uptime indicator shows how long the backend process has been running' },
      { type: 'new', text: 'Feature-rich Sync Log viewer — filterable table with status pills (All/Completed/Failed/Running), date range selector (24h/7d/30d/All), free-text error search, pagination with "Load More", gap highlighting between rows (amber ≥ 30min, red ≥ 1h), click-to-expand detail modal with full error text, and CSV export of the current filtered view' },
      { type: 'new', text: 'Auto-refresh in Sync Operations panel — logs and stats reload every 30 seconds while the section is open so you can watch a live sync progress without manually refreshing' },
      { type: 'new', text: 'CSAT pending count on Sync Health — displays the number of closed/resolved tickets that have no CSAT response yet, giving visibility into the CSAT backlog' },
      { type: 'new', text: 'Startup gap detection and catch-up sync — on every app start (including deployments and restarts), the scheduled sync service now checks how long ago the last successful sync completed; if the gap is over 1 hour it runs a full sync covering gap + 7 days (capped at 90) instead of a normal incremental sync, ensuring no data is missed after idle periods or crashes' },
      { type: 'new', text: 'Azure Always On and health check enabled — App Service is now configured with alwaysOn=true (prevents the process from being killed after 20 minutes of idle) and healthCheckPath="/health" (Azure auto-restarts the instance if the health endpoint stops responding), ensuring cron jobs run continuously 24/7' },
      { type: 'new', text: 'App uptime exposed on health endpoint — GET /health now returns appStartedAt timestamp, shown in the Sync Health panel and useful for diagnosing unexpected restarts' },
      { type: 'improved', text: 'CSAT sync window widened from 30 to 90 days and made configurable — the csat_sync_days app_settings key (default 90) controls how far back each sync checks for new CSAT responses; customers who submit survey feedback weeks after ticket closure will no longer be missed by the 30-day cutoff' },
      { type: 'improved', text: 'Settings sidebar modernized — emoji icons replaced with Lucide icons consistent with the rest of the app; active item uses a white card with blue icon accent instead of a heavy blue fill; inactive items are lighter and subtler; sidebar is slightly narrower for a cleaner layout' },
      { type: 'improved', text: 'Settings tab persists across page refresh — the active section is stored in the URL hash (e.g., /settings#sync-ops); pressing F5 or sharing the URL restores the correct tab; invalid or empty hashes default to FreshService' },
      { type: 'improved', text: 'GET /api/sync/logs API enhanced with pagination, filtering, and total count — supports offset, status, startDate, endDate, and search query params with a pagination envelope ({ total, limit, offset, hasMore }) alongside the data array; max 200 results per page' },
      { type: 'improved', text: 'GET /api/sync/stats API enhanced — now includes longestGap analysis (gapMinutes, gapStart, gapEnd over last 7 days) and csatPendingCount alongside the existing total/completed/failed/successRate fields' },
      { type: 'security', text: 'JWT bearer token fallback for all protected routes — requireAuth middleware now accepts a valid HS256 JWT in the Authorization header when no session cookie is present; this fixes authentication for cross-origin deployments where third-party cookies are blocked (Chrome incognito, Firefox strict mode)' },
      { type: 'security', text: 'SSO login now issues a short-lived JWT alongside the session cookie — the 8-hour JWT is stored in memory (never localStorage) and sent via Authorization header on every API request; cleared on logout' },
      { type: 'security', text: 'SSE EventSource JWT support — since EventSource does not support custom headers, the JWT is passed as a ?token= query parameter; the SSE route middleware promotes it to an Authorization header before requireAuth runs' },
      { type: 'security', text: 'GET /api/auth/session validates JWT bearer token — the session check endpoint now also accepts and validates the JWT so that cookie-less sessions can verify their auth state on page load' },
      { type: 'database', text: 'No schema changes — all improvements are code-level. No migration or prisma db push required for this release.' },
    ],
  },
  {
    version: '1.4.1-preview',
    date: 'March 10, 2026',
    entries: [
      { type: 'fixed', text: 'Infinite 401 loop after SSO login in incognito mode — the auth recovery handler and the token-exchange useEffect could both fire exchangeTokenForSession() concurrently, each creating a new backend session while dashboard API calls raced ahead without the cookie; dozens of SSO logins and 401 errors per second flooded the server, leaving the dashboard stuck on "Loading dashboard..." indefinitely' },
      { type: 'fixed', text: 'Auth recovery now capped at 3 attempts with 2-second cooldown — after exhausting retries the user is redirected to the login page instead of looping; a global exchange lock (isExchangingRef) prevents concurrent token exchanges from any code path' },
      { type: 'fixed', text: 'Session cookie verification after SSO login — exchangeTokenForSession() now calls checkSession() after ssoLogin() to confirm the cookie is actually usable before setting isAuthenticated=true, closing the window where the browser had not yet applied the Set-Cookie header' },
      { type: 'fixed', text: 'Auth callback 5-second fallback redirected unauthenticated users to /dashboard — changed to redirect to /login instead, preventing an unauthenticated render of the dashboard that triggers more 401 cascades' },
      { type: 'fixed', text: 'Background sync never starting on production — server initialization crashed at noiseRuleService.seedDefaults() due to PostgreSQL connection exhaustion ("remaining connection slots are reserved"), and the error was caught at the top level, silently skipping scheduledSyncService.start(); the app served HTTP requests with stale data but ran zero syncs' },
      { type: 'fixed', text: 'noiseRuleService.seedDefaults() failure now non-fatal — wrapped in its own try/catch so a database connection error during noise rule seeding no longer prevents the sync service from starting; a last-resort fallback in the outer catch block also attempts to start the sync service even if other initialization steps fail' },
      { type: 'fixed', text: 'Timeline Explorer crash for technicians with FreshService-format timezones — selecting a tech with timezone "Mountain Time (US & Canada)" threw RangeError: Invalid time zone specified because Intl.DateTimeFormat requires IANA identifiers; affected techs whose records were created before the FRESHSERVICE_TZ_TO_IANA mapping was added, or whose timezone was set via the Visuals schedule API without normalization' },
      { type: 'improved', text: 'Comprehensive timezone normalization with toIANA() — new shared helper in timelineUtils.js maps 27 FreshService/common timezone names to IANA identifiers with Intl.DateTimeFormat validation and safe fallback to America/Los_Angeles; applied in localTimeToUTC(), techConfigsFromTechnician(), and TimelineExplorer techConfigs builder' },
      { type: 'security', text: 'Visuals schedule API now normalizes timezone before persisting — PATCH /api/visuals/agents/:id/schedule maps incoming timezone through FRESHSERVICE_TZ_TO_IANA before writing to the database, preventing non-IANA values from being stored in the future' },
      { type: 'security', text: 'Auth recovery rate limiting prevents server abuse — the 401 handler now enforces a 2-second cooldown between recovery attempts and a hard cap of 3 retries per session, stopping the runaway SSO login flood that previously generated hundreds of backend requests per second' },
      { type: 'database', text: 'No schema changes — all fixes are code-level. No migration or prisma db push required for this release.' },
    ],
  },
  {
    version: '1.2.7-preview',
    date: 'February 27, 2026',
    entries: [
      { type: 'new', text: 'Noise ticket detection system — regex-based rule engine identifies automated/non-actionable tickets (Synology NAS alerts, Hyper-V backup errors, Teams Rooms incidents, vendor spam, monitoring flaps, etc.) and flags them with isNoise; 23 default rules ship out of the box, covering ~20% of all historical tickets' },
      { type: 'new', text: 'Smart dedup window on noise rules — optional dedupWindowDays field lets a rule keep the first occurrence of a recurring alert actionable while suppressing repeats within the window (e.g., Veeam errors use a 7-day window: first alert visible, duplicates hidden)' },
      { type: 'new', text: 'Hide Noise toggle on Dashboard — amber button in the toolbar hides all noise-flagged tickets from stats, counts, and ticket lists; persists in localStorage across sessions; all views (daily, weekly, monthly) and technician detail pages respect the filter' },
      { type: 'new', text: 'Hide Noise toggle on Timeline Explorer — same noise filter available next to the exclude/include filter bar; toggles noise tickets in/out of the avoidance analysis timeline' },
      { type: 'new', text: 'Hide Non-Picked toggle on Timeline Explorer — emerald button filters the timeline to show only tickets picked by the selected agent(s), hiding all not-picked tickets' },
      { type: 'new', text: 'Noise Rules management UI in Settings — new "Noise Rules" section with full CRUD: view all rules with match counts, enable/disable toggles, inline editing of name/pattern/category/description/dedup window, test patterns against the full ticket database with sample matches, add custom rules, delete rules' },
      { type: 'new', text: 'Noise rules backfill — "Re-run Backfill" button in Settings re-evaluates all tickets against current rules; processes chronologically for correct dedup window behavior; updates match counts per rule' },
      { type: 'new', text: 'Noise rules REST API — GET/POST/PUT/DELETE /api/noise-rules for rule management, POST /test to preview pattern matches, POST /backfill to re-tag tickets, POST /seed for initial setup, GET /stats for noise analytics' },
      { type: 'new', text: 'Noise stats dashboard in Settings — 4-card overview showing total tickets, actionable count, noise count, and noise percentage' },
      { type: 'new', text: 'Auto-seeding on startup — noiseRuleService.seedDefaults() runs on server boot, populating the 23 default rules if the noise_rules table is empty; no manual setup required' },
      { type: 'new', text: 'Noise evaluation during sync — every ticket is evaluated against enabled noise rules during FreshService sync, so new tickets are automatically flagged as they arrive' },
      { type: 'improved', text: 'Exclude filters now apply to picked tickets too — previously, excluding a category (e.g., Licensing) only hid not-picked tickets; now picked tickets are also filtered, consistent with user expectation across Timeline Explorer, Coverage tab, and Merged Timeline modal' },
      { type: 'improved', text: 'Debounced text inputs in filter bars — exclude/include keyword inputs now wait 400ms after the last keystroke before triggering a re-filter, eliminating jittery re-renders on every keypress' },
      { type: 'fixed', text: 'Timeline Explorer crash (RangeError: Invalid time value) when noise filter removes all tickets for a day — buildTimeline now returns early for empty day arrays, and localTimeToUTC guards against undefined/invalid date strings' },
      { type: 'fixed', text: 'Timeline Explorer noise toggle had no effect — /api/dashboard/timeline endpoint was not reading the excludeNoise query parameter; avoidance analysis functions (loadTicketsFull) were querying all tickets directly without noise filtering; both now thread excludeNoise through the full call chain' },
      { type: 'security', text: 'All noise rule API endpoints require authentication (requireAuth middleware) — no public access to rule management or ticket data' },
      { type: 'security', text: 'Regex pattern validation on create/update — invalid patterns are rejected with a descriptive error before being stored, preventing ReDoS or runtime crashes from malformed expressions' },
      { type: 'database', text: 'New table: noise_rules (id, name, pattern, description, category, is_enabled, match_count, dedup_window_days, created_at, updated_at) with index on is_enabled' },
      { type: 'database', text: 'New columns on tickets: is_noise (BOOLEAN DEFAULT false, indexed) and noise_rule_matched (VARCHAR 100, nullable)' },
      { type: 'database', text: 'Run `prisma db push` or `prisma migrate deploy` on production before deploying. After deploy, call POST /api/noise-rules/seed to populate rules, then POST /api/noise-rules/backfill to tag existing tickets (~12k tickets takes ~15 seconds).' },
    ],
  },
  {
    version: '1.2.6-preview',
    date: 'February 27, 2026',
    entries: [
      { type: 'new', text: 'Redesigned dashboard stats bar — two-zone flex layout replaces the old 3-column grid; date navigation anchored on the left, all stats + self-pick progress + view toggle on the right; cleaner separation between controls and data' },
      { type: 'new', text: 'View toggle (Daily / Weekly / Monthly) moved to far right of the stats bar — treated as a mode selector alongside the data, not mixed into the navigation zone; consistent with standard UI patterns' },
      { type: 'new', text: 'Compact view toggle moved into the Technicians section — Cards / Compact segmented control now lives above the tech list where it contextually belongs, removed from the top header' },
      { type: 'new', text: 'Timeline Explorer redesigned to match the Dashboard header — white top bar (Back, title, tech count, picked/not-picked totals, refresh) + gradient nav bar (date nav on left, Day by Day/Combined + Daily/Weekly/Monthly on right)' },
      { type: 'new', text: 'Scoped ticket loading (getAllActiveScoped) — backend now fetches only tickets relevant to the requested date range plus open/pending and CSAT records, instead of loading every ticket for every technician on every request' },
      { type: 'new', text: 'Speculative cache warmup on page load — DashboardContext prefetches current week, today, and weekly stats immediately on mount using a _speculative flag that suppresses 401 auth redirects, so data is ready before the first render' },
      { type: 'new', text: 'Shared Prisma singleton (prisma.js) — all services and routes now share one PrismaClient instance instead of creating one per module, eliminating connection pool exhaustion under load' },
      { type: 'improved', text: 'Dashboard white header is now sticky (z-40) — stays visible at the top while scrolling through the technician list' },
      { type: 'improved', text: 'Gradient stats bar is now sticky (z-30, top-[52px]) — docks directly below the sticky white header; keeps date context, stats, and view controls visible when scrolling' },
      { type: 'improved', text: 'Default view on first load changed from Daily to Weekly — weekly overview gives a better immediate picture of team workload for coordinators arriving each morning' },
      { type: 'improved', text: 'Stats and avoidance analysis run in parallel (Promise.all) — daily and weekly dashboard endpoints no longer wait for avoidance analysis to finish before returning stats; reduces response latency' },
      { type: 'improved', text: 'Background sync status polling deferred until primary data arrives — prevents unnecessary /sync/status requests during the cold load phase, reducing startup network chatter' },
      { type: 'improved', text: 'Adjacent-period prefetch gated on primaryDataReady — usePrefetch no longer fires before the active view has rendered, preventing redundant fetches that compete with the primary request' },
      { type: 'improved', text: 'Sync buttons simplified — removed dropdown wrapper; Sync All and Sync Week are now plain paired icon buttons with cleaner disabled state (opacity-40 instead of opacity-50)' },
      { type: 'improved', text: 'Speculative 401 errors silenced — requests fired during the auth-check window carry a _speculative flag; if they get a 401, the auth:unauthorized event is suppressed so the login redirect is not triggered incorrectly' },
      { type: 'fixed', text: 'Multiple PrismaClient instances created on each request — avoidanceAnalysisService, technicianRepository, and weekly/monthly route handlers each instantiated their own PrismaClient; now all share the singleton, fixing potential connection limit errors on Azure' },
      { type: 'fixed', text: 'Weekly/monthly dashboard routes manually constructing and disconnecting PrismaClient inline — removed all ad-hoc instantiation and $disconnect() calls; shared client manages its own lifecycle' },
      { type: 'database', text: 'New index: tickets_first_assigned_at_idx on first_assigned_at — required by the scoped ticket query (OR condition on firstAssignedAt range). Migration file included (20260227000000_add_first_assigned_at_index). Run `prisma migrate deploy` on production before deploying this version.' },
    ],
  },
  {
    version: '1.2.5-preview',
    date: 'February 27, 2026',
    entries: [
      { type: 'new', text: 'Monthly view on technician detail page — Daily/Weekly/Monthly toggle in header, with month picker, calendar-grid daily breakdown (7-column Mon–Sun), monthly summary stats, averages, assigners, and CSAT; works across Overview, Tickets, Coverage, and Timeline' },
      { type: 'new', text: 'Monthly coverage analysis — new backend endpoint and service (computeTechnicianAvoidanceMonthlyDetail) returns all weekday coverage windows for the full month in a single query; Coverage tab shows a 5-column multi-row weekday grid for monthly breakdown' },
      { type: 'new', text: 'Timeline Explorer — standalone page (/timeline) with multi-technician selection, daily/weekly/monthly period controls, Day by Day / Combined view toggles, full filter bar (exclude + include), and per-agent color-coded ticket rows' },
      { type: 'new', text: 'Multi-technician timeline — select 1 or more techs from a sidebar panel; each tech gets a distinct accent color; picked tickets show which agent picked them; markers show per-agent online/offline times sorted chronologically' },
      { type: 'new', text: 'Timeline Explorer sidebar — collapsible tech selector with smooth animation; search, select all/none, per-tech picked/not-picked stats inline; selected techs sort to top alphabetically; collapsed mode shows avatars with stats' },
      { type: 'new', text: 'Hide agents — eye button on each tech in Timeline Explorer to hide inactive agents; hidden techs collapse to a bottom section; show/hide persisted in sessionStorage' },
      { type: 'new', text: 'Empty day collapsing — consecutive days with no matching tickets in Day by Day rolling view are collapsed into a single compact dashed separator line (e.g., "6 days · Feb 6 – Feb 13 · no matching tickets")' },
      { type: 'new', text: 'Batch timeline API endpoint — GET /api/dashboard/timeline accepts multiple techIds and a period (date/weekStart/month); runs avoidance analysis in parallel for all selected techs; 30-second read cache' },
      { type: 'new', text: 'Timeline button on main Dashboard header — blue outlined button with Layers icon navigates directly to /timeline' },
      { type: 'new', text: 'Full-date ticket timestamps in multi-day views — ticket rows show "1-16 Fri. 3:03 AM" format in weekly/monthly instead of ambiguous "Fri 3:03 AM"' },
      { type: 'improved', text: 'Timeline markers are compact — "REZA ONLINE — 08:00 VANCOUVER" shortened to "Reza on · 8am Van"; city names abbreviated (Vancouver→Van, Toronto→Tor, etc.); marker colors (green/blue/red) carry the meaning' },
      { type: 'improved', text: 'Timeline markers sorted by time — when no tickets trigger markers (empty days), remaining agent on/HQ on/agent off markers now sort chronologically instead of code-insertion order' },
      { type: 'improved', text: 'Header toggle layout stabilized — Daily/Weekly/Monthly buttons, date picker, Next button, and Today/This Week/This Month button all use fixed-width slots and invisible placeholders instead of conditional rendering, eliminating layout shifts when switching views' },
      { type: 'improved', text: 'Coverage filter bar moved to full width — filter bar now spans above both Picked and Not Picked columns so it is clearly a shared filter, not just for the right column' },
      { type: 'improved', text: 'Coverage Timeline button relocated — moved from the Picked column header to a tall button alongside the summary cards for better visibility' },
      { type: 'improved', text: 'MergedTimelineModal fully refactored — ~490 lines reduced to ~120 by extracting all rendering and logic into shared timeline/* modules (TimelineCore, TimelineTicketRow, TimelineSeparators, timelineUtils); identical visual behavior' },
      { type: 'improved', text: 'Loading overlay on Timeline Explorer — same fancy multi-ring spinner with backdrop blur as the main Dashboard, shown during initial tech list load and all data fetches' },
      { type: 'fixed', text: 'Monthly coverage timeline cut off at 14 days — getWeekdaysInRange had a hardcoded loop limit of 14 iterations; now dynamically computed from the actual date range to cover any month' },
      { type: 'fixed', text: 'Per-tech picked count mismatch — stats used _techFirstName string matching which missed extended tickets; fixed to use assignedTechId for accurate counting' },
    ],
  },
  {
    version: '1.2.1-preview',
    date: 'February 26, 2026',
    entries: [
      { type: 'new', text: 'Technician detail page fully redesigned — 2,500-line monolith refactored into 9 focused sub-components (TechDetailHeader, MetricsRibbon, OverviewTab, TicketBoardTab, CoverageTab, CSATTab, MergedTimelineModal, FilterBar, and shared constants/utils)' },
      { type: 'new', text: '4-tab structure replaces 7 flat tabs — Overview, Tickets, Coverage, and CSAT; the Tickets tab has its own sub-navigation pills (All Open, Self-Picked, Assigned, Closed)' },
      { type: 'new', text: 'Compact single-row sticky header — tech identity (photo, name, location, timezone, schedule) on the left; Daily/Weekly toggle, date navigation, and export all grouped on the right' },
      { type: 'new', text: 'Slim metrics ribbon — replaces the 5 large clickable stat cards with a single unclickable horizontal row of key figures; ribbon lives inside the Tickets tab where it is contextually relevant' },
      { type: 'new', text: 'Ticket list redesigned as a strict CSS-grid table — columns for Priority, Ticket #, Subject/Requester, Status, Category, and Time Metric; alternating row backgrounds for scanability; "badge diet" reduces visual noise' },
      { type: 'new', text: 'Include filter added to Coverage and Merged Timeline — one-line filter bar with [Exclude ▼] [exclude text] | [include text] [Include ▼]; include filter shows only tickets matching the specified keywords or categories' },
      { type: 'new', text: 'OR syntax in both filter inputs — use `|` to combine multiple terms (e.g. "cpu | printer | vpn" matches any ticket containing any of those words)' },
      { type: 'new', text: 'Category filter dropdowns replace pill walls — compact popovers with checkbox lists for both Exclude and Include; no more horizontal scrolling wall of pills in the Coverage and Timeline filter bars' },
      { type: 'new', text: 'Day by Day / Combined toggle in Merged Timeline weekly view — "Day by Day" shows tickets grouped and separated by date with per-day headers; "Combined" collapses all days to time-of-day order across the whole week' },
      { type: 'new', text: 'Weekend day separators in combined Merged Timeline — automatic divider lines mark date boundaries, labelled with weekday name and holiday info if applicable' },
      { type: 'new', text: 'Coverage analysis now available for all technicians — removed Eastern/Atlantic-only restriction; Pacific, Mountain, Central, and other timezones now show full coverage data' },
      { type: 'new', text: 'Future days blocked from navigation — Next Day / Next Week buttons hidden when already on today or the current week; date picker max is set to today; Coverage timeline filters out any future days automatically' },
      { type: 'improved', text: 'Overview tab visually distinct from other tabs — subtle background tint on active Overview, making it clear it is an analytics view rather than a data list' },
      { type: 'improved', text: 'Overview stat rows are compact — replaced tall metric boxes with a tight horizontal StatRow; large numbers with small labels replaced by balanced size hierarchy' },
      { type: 'improved', text: 'Weekly Overview order corrected — Weekly Summary and Self-Picked vs Assigned bar now appear above the Daily Breakdown grid instead of below it' },
      { type: 'improved', text: 'Merged Timeline stays open during date navigation — clicking Previous, Next, or Today inside the timeline modal no longer closes and reopens the modal; data refreshes in place' },
      { type: 'improved', text: 'Semantic color palette applied throughout — decorative purple/orange/yellow replaced with a strict system: red for Open/Urgent, amber for Pending, emerald for Resolved/Closed/picked, slate for neutral' },
      { type: 'improved', text: 'Coverage filter shared between the Coverage tab and Merged Timeline — any exclude/include change made in one view is immediately reflected in the other without resetting' },
      { type: 'fixed', text: 'Agent online/offline markers in Merged Timeline showing incorrect times for non-Eastern timezones — rewritten to use Intl.DateTimeFormat for DST-accurate UTC offset calculation (fixes Mountain Time / Edmonton and similar)' },
      { type: 'fixed', text: 'Merged Timeline closing on prev/next navigation — removed the erroneous onClose() calls from the navigation button handlers inside the modal' },
      { type: 'fixed', text: 'Coverage Daily Breakdown and timeline including tomorrow and future days when viewing the current week — future dates are now filtered before rendering' },
    ],
  },
  {
    version: '1.2.0-preview',
    date: 'February 25, 2026',
    entries: [
      { type: 'new', text: 'Coverage Analysis tab on technician detail — shows overnight eligible tickets (previous business day 5 PM PT → 9 AM PT) split into "Picked by tech" vs "Not Picked" with full ticket lists' },
      { type: 'new', text: 'Merged Timeline modal — chronological view of all overnight + daytime tickets with picked (green) vs not-picked (grey/dimmed) visual coding, filterable in real time' },
      { type: 'new', text: 'Timeline markers — "Agent Start", "Vancouver Online (9 AM PT)", and "Agent End" separator lines inserted at correct chronological positions in the merged timeline' },
      { type: 'new', text: 'Extended timeline to 5 PM PT — merged view now shows tickets arriving after 9 AM through end of business day, separated by the HQ-online marker' },
      { type: 'new', text: 'Overnight vs early-morning ticket indicators — moon icon for tickets arriving before 5 AM ET, sunrise icon for 5 AM ET and later, with subtle background tinting' },
      { type: 'new', text: 'Exclude filters on "Not Picked" — keyword search and clickable category pills to hide irrelevant overnight tickets; click any category badge on a ticket row to instantly exclude that category' },
      { type: 'new', text: 'Prev/Next navigation inside merged timeline modal — arrow buttons to step through days or weeks without closing the modal' },
      { type: 'new', text: 'Wait-time metric on coverage tickets — purple badge showing how long each ticket sat before first assignment (e.g., "⏱ 2h15m")' },
      { type: 'new', text: 'Per-technician work schedules — new Settings page section to configure each tech\'s start time, end time, and timezone with one-click segmented controls' },
      { type: 'new', text: 'PATCH /api/visuals/agents/:id/schedule endpoint — save per-tech work hours and timezone independently of FreshService sync' },
      { type: 'new', text: 'FreshService timezone normalization — human-readable timezone strings (e.g., "Pacific Time (US & Canada)") automatically mapped to IANA identifiers on sync and in the UI' },
      { type: 'new', text: 'Coverage analysis backend service — avoidanceAnalysisService.js computes overnight eligible/picked/not-picked ticket lists per tech with extended-day ticket loading' },
      { type: 'improved', text: 'Coverage analysis scoped to Eastern/Atlantic technicians only — HQ (Pacific) techs see a clear "not applicable" message instead of empty data' },
      { type: 'improved', text: 'Tech sync no longer overwrites manually-set timezones — upsert preserves timezone, location, showOnMap, and isMapManager fields' },
      { type: 'improved', text: 'Two-column side-by-side layout for picked vs not-picked tickets with both columns flush at the top' },
      { type: 'improved', text: 'Ticket rows are single-line compact — ticket number removed (link icon retained), subject truncates, all metadata on one row' },
      { type: 'improved', text: 'Agents endpoint now supports includeInactive query parameter for the schedules settings page' },
      { type: 'fixed', text: 'Removed old avoidance badge/scoring system (Flagged/Watch/Good) from dashboard cards and settings — replaced by transparent ticket-list approach' },
      { type: 'database', text: 'New columns: technicians.work_start_time (VARCHAR 5) and technicians.work_end_time (VARCHAR 5) — nullable, migration included, uses IF NOT EXISTS for safe re-run' },
    ],
  },
  {
    version: '1.1.2-preview',
    date: 'February 23, 2026',
    entries: [
      { type: 'new', text: 'Expandable ticket drilldown in compact view — click the chevron on any technician row to see all tickets for the period, with self-picked tickets highlighted and closed tickets shown in a muted style' },
      { type: 'new', text: 'Ticket drilldown works in daily, weekly, and monthly compact views — sections automatically label as "Today\'s tickets", "This week\'s tickets", or "This month\'s tickets"' },
      { type: 'new', text: 'Closed/Resolved section in drilldown — tickets that were closed appear in both the main list and a dedicated greyed-out section below a divider' },
      { type: 'new', text: 'Expand All / Collapse All button — appears in compact view header to toggle all technician drilldowns at once' },
      { type: 'new', text: 'Centralized ExpandableTicketList component — single source of truth for ticket row rendering, grouping, and styling across all views' },
      { type: 'new', text: 'Automatic session recovery on 401 — expired sessions silently re-authenticate via Azure AD token exchange instead of showing a dead "Authentication required" screen' },
      { type: 'improved', text: 'SSE reconnection with exponential backoff — connection status stays "Connecting..." during retries instead of falsely showing "Offline" on first load' },
      { type: 'improved', text: 'Ticket drilldown rows show full requester name and email in brackets (previously only showed first name)' },
      { type: 'improved', text: 'Ticket drilldown sorted by arrival time (newest first) with date shown in weekly/monthly views (e.g., "Mon, 2/23 11:42 AM")' },
      { type: 'improved', text: 'Error retry button on dashboard now retries loading the current view instead of triggering a full FreshService sync' },
      { type: 'fixed', text: 'Weekly ticket counts showing 0 on current week — weeklyTickets filter now uses timezone-aware boundaries (America/Los_Angeles) consistent with daily breakdown calculations' },
      { type: 'fixed', text: 'SSE connection getting stuck on "Offline" after auth/network hiccup — EventSource now auto-recreates with backoff instead of staying closed' },
      { type: 'fixed', text: '"Authentication required" screen with broken Retry button — 401 errors now trigger silent token re-exchange or redirect to login' },
    ],
  },
  {
    version: '1.1.0-preview',
    date: 'February 22, 2026',
    entries: [
      { type: 'new', text: 'Technician detail Overview tab — period performance summary with metrics, daily breakdown, self-pick rate, assigners, and category breakdown' },
      { type: 'new', text: 'Daily/Weekly toggle on technician detail page — switch views without navigating back to the dashboard' },
      { type: 'new', text: 'Clickable day boxes in weekly view — click a day on any technician\'s weekly breakdown to jump directly to that tech\'s daily detail' },
      { type: 'new', text: 'Clickable mini-calendar in weekly mode — click any day in the top calendar bar to switch to that day\'s daily view' },
      { type: 'new', text: 'Day-of-month numbers shown on weekly calendars — both the dashboard mini-calendar and per-technician weekly breakdown now display date numbers (e.g., Mon 19, Tue 20)' },
      { type: 'new', text: 'Backend read cache middleware — server-side response caching (10-15s TTL) on all dashboard endpoints, cleared automatically on sync' },
      { type: 'new', text: 'Frontend data cache with stale-while-revalidate, request deduplication, and LRU eviction' },
      { type: 'new', text: 'Hover prefetch on technician cards — data is preloaded when hovering over a technician card for faster navigation' },
      { type: 'improved', text: 'Loading overlay now appears when switching dates in daily mode (previously only showed for weekly/monthly switches)' },
      { type: 'improved', text: 'Weekly summary metrics redesigned — Total and Closed counts are now visually highlighted with bold filled cards; other metrics use a simplified neutral palette' },
      { type: 'improved', text: 'Day box hover animation — individual day boxes in weekly breakdown scale up with a blue ring on hover for clear click affordance' },
      { type: 'improved', text: 'Back navigation preserves origin view — clicking a day in weekly view and pressing Back correctly returns to weekly dashboard, not daily' },
      { type: 'fixed', text: 'SSE connection shows "Connecting..." instead of false "Offline" during initial page load — added three-state connection status (connecting/connected/disconnected)' },
      { type: 'fixed', text: 'Technician detail crash on navigation — added null guard in getInitials() and race-safe fetch with sequence tracking' },
      { type: 'fixed', text: 'Weekly technician detail showing 0s on first load — bypassed frontend cache for weekly detail fetch to avoid stale data from cache dedup race condition' },
      { type: 'fixed', text: 'Week range display stuck on "Loading..." — falls back to local selectedWeek state when API weekStart field is missing' },
    ],
  },
  {
    version: '1.0.0',
    date: 'February 18, 2026',
    entries: [
      { type: 'new', text: 'Real-time IT helpdesk dashboard with SSE live updates' },
      { type: 'new', text: 'Daily, weekly, and monthly ticket views with calendar navigation' },
      { type: 'new', text: 'Self-picked vs assigned ticket detection algorithm' },
      { type: 'new', text: 'Smart search with OR/AND operators and category filtering' },
      { type: 'new', text: 'Azure AD SSO authentication replacing password login' },
      { type: 'new', text: 'Azure deployment with App Service, Static Web Apps, and PostgreSQL' },
      { type: 'new', text: 'GitHub Actions CI/CD for automated backend and frontend deployments' },
      { type: 'new', text: 'Profile photo sync from Azure AD (Entra ID)' },
      { type: 'new', text: 'CSAT score tracking with feedback display' },
      { type: 'new', text: 'Technician map visualization page' },
      { type: 'new', text: 'Excel/XLSX ticket export with formatting' },
      { type: 'new', text: 'LLM-powered auto-response configuration panel' },
      { type: 'improved', text: 'Activity analysis progress logging every 50 tickets during sync' },
      { type: 'improved', text: 'Compact and grid view modes for technician cards' },
      { type: 'fixed', text: 'Timezone bug causing tomorrow\'s tickets to appear in Pacific evening' },
      { type: 'fixed', text: 'SPA routing 404 on page refresh with staticwebapp.config.json' },
      { type: 'fixed', text: 'Cross-origin session cookies for Static Web App to App Service' },
      { type: 'fixed', text: 'Database schema drift resolved with catch-all migration' },
      { type: 'fixed', text: 'Photo sync using incorrect API base URL on production' },
    ],
  },
];
